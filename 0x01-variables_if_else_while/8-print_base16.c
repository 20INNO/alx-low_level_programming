#include<stdio>

/**
 * main - prints all the numbers of base 16 in lowercase.
 *
 * Return: (0).
 */
int main(void)
{
	char ch;
	int num;

	for (num = 0; num < 16; num++)
		putchar((num % 16 + '0');

		putchar(ch);

		putchar('\n');

		return (0);
}


