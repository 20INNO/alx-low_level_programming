#include<stdlib.h>
#include<stdio.h>
/**
 * main - prints the alphabet in lowercase, and then in uppercase
 * Return: (0)
 */
int main(void)
{
	char alphaletter;

	for (alphaletter = 'a'; letter <= 'z'; alphaletter++)
		putchar(alphaletter);

	for (alphaletter = 'A'; alphaletter <= 'Z'; letter++)
		putchar(alphaletter);

	putchar('\n');

	return (0);
