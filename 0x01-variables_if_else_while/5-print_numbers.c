#include<stdio.h>

/**
 * main -  prints all single digit numbers of base 10 starting from 0.
 *
 * return (0)
 */
{
	int digitnum;

	for (digitnum = 0; digitnum < 10; digitnum++)
		printf("%d", digitnum);

	printf("\n");

	return (0);
}
